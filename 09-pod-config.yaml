kind: Pod
apiVersion: v1
metadata:
  name: pod-config 
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: daws78s
    # env:
    # - name: course-1
    #   valueFrom:
    #     configMapKeyRef:
    #       name: daws78s
    #       key: course
    # - name: trainer-1
    #   valueFrom:
    #     configMapKeyRef:
    #       name: daws78s
    #       key: trainer